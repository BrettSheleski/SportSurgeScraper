
import { Router, Request, Response } from "express";
import ControllerBase from "./ControllerBase";

class SetChannelController extends ControllerBase {
    // protected onInitializeRoutes(router: Router, path: string): void {
    //     router.post(path, this.saveChannel);
    // }

    // async saveChannel(request: Request, response: Response) : Promise<void> {

    // }
}

export default SetChannelController;